body {
  margin: 0;
  padding: 0;
  font-family: sans-serif;
}
.row::after{
	display: block;
	content: "";
	clear: both;
}
.types-col, .search-col{
	width: 50%;
	float: left;
	box-sizing: border-box;
	padding: 10px;
}